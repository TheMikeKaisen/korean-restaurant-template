import type { Metadata } from 'next'
import { MenuPageContent } from '@/components/sections/MenuPageContent'
export const metadata: Metadata = { title: 'Menu — Gangnam Kitchen' }
export default function MenuPage() { return <MenuPageContent /> }
